const slime_atributos_base = {
    hpMax: 20,
    ataque: 10,
    velocidade: 1,
    velocidadeMaxima: 70,
    raioAtaque: 5,
    cooldownAtaque: 0,
    cooldownImune: 0,
}

const slime_crescimento_por_nivel = {
    hpMax: 1.3,
    ataque: 1.3,
    velocidade: 1.1,
    raioAtaque: 1,
    cooldownAtaque: 1.5,
    cooldownImune: 1.5,
}
export { slime_atributos_base, slime_crescimento_por_nivel };